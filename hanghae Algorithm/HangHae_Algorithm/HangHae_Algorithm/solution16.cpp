//문자열 내 p와 y의 개수
#include<iostream>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	return 0;
}
/*
#include <string>
#include <iostream>
using namespace std;
int pcount = 0, ycount = 0;

bool solution(string s)
{
    bool answer = true;

    for(int i = 0; i < s.length(); i++)
    {
        if(s[i] == stricmp('p'))
            pcount++;
        else if(s[i] == stricmp('y'))
            ycount++;
    }
    if(pcount != ycount)
        answer = false;

    return answer;
}
*/